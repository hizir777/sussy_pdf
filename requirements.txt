# === Sussy PDF - Bağımlılıklar (Pinned) ===
# Deterministic deployment — her ortamda aynı sürümler

# PDF Analiz
pdfminer.six==20251230
PyPDF4==3.0.1  # PDF encryption/decryption support

# Dosya Tipi Tespiti
python-magic-bin==0.4.14 ; sys_platform == 'win32'
python-magic==0.4.27 ; sys_platform != 'win32'

# YARA Kural Motoru (opsiyonel — MSVC gerektirir)
# yara-python==4.5.1  # pip install yara-python (C++ Build Tools gerekli)

# HTTP İstekleri (VirusTotal API vb.)
requests==2.33.0

# Çevresel Değişkenler
python-dotenv==1.0.1
pydantic==2.0.0  # Settings configuration
pydantic-settings==2.0.0  # .env file management

# CLI Arayüzü
rich==13.9.4
click==8.1.7

# Dashboard API
fastapi==0.135.3
starlette==0.49.1
uvicorn==0.34.0
python-multipart==0.0.22

# Güvenlik & Kimlik Doğrulama (v1.1.0+)
python-jose==3.3.0  # JWT tokens
cryptography==43.0.0  # Encryption, secure key management
pycryptodome==3.21.0  # AES encryption for PDF password-protected files

# Rate Limiting (v1.1.0+)
slowapi==0.1.9  # Async-compatible rate limiter

# Database / Future Support (v1.1.0+)
sqlalchemy==2.0.31  # ORM for audit logging
alembic==1.15.0  # Database migrations

# Async Improvements (v1.2.0+)
aiofiles==23.2.1  # Async file I/O

# === Geliştirme Bağımlılıkları ===
# pip install -r requirements.txt
# Sonra development tools:
# pip install pytest pytest-cov ruff mypy httpx
